[1mdiff --git a/.env.example b/.env.example[m
[1mindex 9d5817d..be39af8 100644[m
[1m--- a/.env.example[m
[1m+++ b/.env.example[m
[36m@@ -1,8 +1,7 @@[m
 # Environment variables for Find[m
 [m
 # Database[m
[31m-DATABASE_URL=postgresql://find:find123@db:5432/find[m
[31m-[m
[32m+[m[32mDATABASE_URL=postgresql://find:find123@localhost:5432/find[m
 # MinIO Object Storage[m
 MINIO_ROOT_USER=minioadmin[m
 MINIO_ROOT_PASSWORD=minioadmin[m
[1mdiff --git a/backend/src/find_api/core/config.py b/backend/src/find_api/core/config.py[m
[1mindex 84d7511..dd3e8d9 100644[m
[1m--- a/backend/src/find_api/core/config.py[m
[1m+++ b/backend/src/find_api/core/config.py[m
[36m@@ -33,6 +33,7 @@[m [mclass Settings(BaseSettings):[m
 [m
     # ML Models[m
     ML_MODE: Literal["full", "mock"] = "full"[m
[32m+[m[32m    ML_MODEL_IDLE_TTL_SECONDS: int = 600[m
     CLIP_MODEL: str = "ViT-B-16-SigLIP"[m
     CLIP_PRETRAINED: str = "webli"[m
     BLIP_MODEL: str = "microsoft/Florence-2-base"[m
[1mdiff --git a/backend/src/find_api/core/model_manager.py b/backend/src/find_api/core/model_manager.py[m
[1mindex 4bcebca..181986d 100644[m
[1m--- a/backend/src/find_api/core/model_manager.py[m
[1m+++ b/backend/src/find_api/core/model_manager.py[m
[36m@@ -3,8 +3,16 @@[m [mModel Manager for efficient GPU resource management[m
 """[m
 [m
 import asyncio[m
[32m+[m[32mimport gc[m
 import logging[m
[31m-from typing import Any, Callable, Dict[m
[32m+[m[32mimport threading[m
[32m+[m[32mimport time[m
[32m+[m[32mfrom typing import Any, Callable, Dict, List[m
[32m+[m
[32m+[m[32mtry:[m
[32m+[m[32m    import torch[m
[32m+[m[32mexcept ImportError:[m
[32m+[m[32m    torch = None[m
 [m
 logger = logging.getLogger(__name__)[m
 [m
[36m@@ -13,6 +21,7 @@[m [mclass ModelManager:[m
     """[m
     Singleton class to manage ML models and GPU resources.[m
     Ensures that heavy GPU tasks are serialized to prevent OOM on 4GB VRAM.[m
[32m+[m[32m    Also supports lazy-loading and idle unloading to save memory.[m
     """[m
 [m
     _instance = None[m
[36m@@ -28,9 +37,32 @@[m [mclass ModelManager:[m
             return[m
 [m
         self.models: Dict[str, Any] = {}[m
[32m+[m[32m        self.last_used: Dict[str, float] = {}[m
[32m+[m[32m        self._lock = threading.Lock()[m
         self.gpu_lock = asyncio.Lock()[m
[32m+[m[32m        self._cleanup_thread = None[m
         self._initialized = True[m
[31m-        logger.info("ModelManager initialized with GPU Lock")[m
[32m+[m[32m        logger.info("ModelManager initialized with GPU Lock and Lazy Loading support")[m
[32m+[m
[32m+[m[32m    def start_autocleanup(self, interval_seconds: int = 60, ttl_seconds: int = 600):[m
[32m+[m[32m        """Start background thread for automatic idle unloading"""[m
[32m+[m[32m        with self._lock:[m
[32m+[m[32m            if self._cleanup_thread and self._cleanup_thread.is_alive():[m
[32m+[m[32m                return[m
[32m+[m
[32m+[m[32m            def cleanup_loop():[m
[32m+[m[32m                logger.info([m
[32m+[m[32m                    f"Background ML model cleanup started (interval={interval_seconds}s, ttl={ttl_seconds}s)"[m
[32m+[m[32m                )[m
[32m+[m[32m                while True:[m
[32m+[m[32m                    try:[m
[32m+[m[32m                        time.sleep(interval_seconds)[m
[32m+[m[32m                        self.unload_idle_models(ttl_seconds)[m
[32m+[m[32m                    except Exception as e:[m
[32m+[m[32m                        logger.error(f"Error in model cleanup thread: {e}")[m
[32m+[m
[32m+[m[32m            self._cleanup_thread = threading.Thread(target=cleanup_loop, daemon=True)[m
[32m+[m[32m            self._cleanup_thread.start()[m
 [m
     async def acquire_lock(self):[m
         """Acquire GPU lock"""[m
[36m@@ -56,16 +88,56 @@[m [mclass ModelManager:[m
         Returns:[m
             The model instance[m
         """[m
[31m-        if name not in self.models:[m
[31m-            logger.info(f"Loading model: {name}")[m
[31m-            try:[m
[31m-                self.models[name] = loader()[m
[31m-                logger.info(f"Model loaded successfully: {name}")[m
[31m-            except Exception:[m
[31m-                logger.exception("Failed to load model %s", name)[m
[31m-                raise[m
[32m+[m[32m        with self._lock:[m
[32m+[m[32m            if name not in self.models:[m
[32m+[m[32m                logger.info(f"Lazy-loading model: {name}")[m
[32m+[m[32m                try:[m
[32m+[m[32m                    self.models[name] = loader()[m
[32m+[m[32m                    logger.info(f"Model loaded successfully: {name}")[m
[32m+[m[32m                except Exception:[m
[32m+[m[32m                    logger.exception("Failed to load model %s", name)[m
[32m+[m[32m                    raise[m
[32m+[m
[32m+[m[32m            self.last_used[name] = time.time()[m
[32m+[m[32m            return self.models[name][m
[32m+[m
[32m+[m[32m    def unload_idle_models(self, ttl_seconds: int):[m
[32m+[m[32m        """[m
[32m+[m[32m        Unload models that haven't been used for ttl_seconds.[m
[32m+[m[32m        """[m
[32m+[m[32m        now = time.time()[m
[32m+[m[32m        to_unload = [][m
[32m+[m
[32m+[m[32m        with self._lock:[m
[32m+[m[32m            for name, last_ts in self.last_used.items():[m
[32m+[m[32m                if name in self.models and (now - last_ts) > ttl_seconds:[m
[32m+[m[32m                    to_unload.append(name)[m
 [m
[31m-        return self.models[name][m
[32m+[m[32m            if not to_unload:[m
[32m+[m[32m                return[m
[32m+[m
[32m+[m[32m            for name in to_unload:[m
[32m+[m[32m                logger.info([m
[32m+[m[32m                    f"Unloading idle model: {name} (idle for {now - self.last_used[name]:.1f}s)"[m
[32m+[m[32m                )[m
[32m+[m[32m                del self.models[name][m
[32m+[m[32m                del self.last_used[name][m
[32m+[m
[32m+[m[32m        # Force garbage collection outside the lock to avoid blocking other threads[m
[32m+[m[32m        gc.collect()[m
[32m+[m
[32m+[m[32m        # Clear CUDA cache if possible[m
[32m+[m[32m        if torch is not None and torch.cuda.is_available():[m
[32m+[m[32m            try:[m
[32m+[m[32m                torch.cuda.empty_cache()[m
[32m+[m[32m                logger.debug("CUDA cache cleared after model unloading")[m
[32m+[m[32m            except Exception as e:[m
[32m+[m[32m                logger.warning(f"Failed to clear CUDA cache: {e}")[m
[32m+[m
[32m+[m[32m    def get_loaded_models(self) -> List[str]:[m
[32m+[m[32m        """Get list of currently loaded model names"""[m
[32m+[m[32m        with self._lock:[m
[32m+[m[32m            return list(self.models.keys())[m
 [m
 [m
 # Global instance[m
[1mdiff --git a/backend/src/find_api/main.py b/backend/src/find_api/main.py[m
[1mindex 0493154..5d35bbe 100644[m
[1m--- a/backend/src/find_api/main.py[m
[1m+++ b/backend/src/find_api/main.py[m
[36m@@ -11,6 +11,8 @@[m [mimport logging[m
 from find_api.core.database import init_db[m
 from find_api.core.recovery import run_analysis_recovery_loop[m
 from find_api.core.storage import init_storage[m
[32m+[m[32mfrom find_api.core.model_manager import get_model_manager[m
[32m+[m[32mfrom find_api.core.config import settings[m
 from find_api.routers import upload, gallery, search, clusters, status, cluster, people[m
 [m
 # Configure logging[m
[36m@@ -43,6 +45,12 @@[m [masync def lifespan(app: FastAPI):[m
     logger.info("Initializing MinIO storage...")[m
     init_storage()[m
 [m
[32m+[m[32m    # Start ML model cleanup[m
[32m+[m[32m    logger.info("Starting ML model cleanup thread...")[m
[32m+[m[32m    get_model_manager().start_autocleanup([m
[32m+[m[32m        ttl_seconds=settings.ML_MODEL_IDLE_TTL_SECONDS[